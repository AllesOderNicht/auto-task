Invoke the `harness-task:check` skill to perform a pre-execution sanity check on the current development change.
Pass a git branch name as an argument, or omit it to use the current branch.
This command is intended to be run after `prompt.md`, `proposal.md`, and all `phases/PH-*.md` files are complete but before any code has been written.
