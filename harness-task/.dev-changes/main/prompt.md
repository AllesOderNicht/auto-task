# Prompt

- Branch: `main`

## Requirement

Proposal 阶段需要和 outlining 阶段完全合并，新的流程约束如下：

- 只保留 `outlining` 阶段，移除 `proposing`
- proposal 与 outline 的流程和文档产物合并
- 只保留一次用户确认点
- `/alles-dev` 一开始就创建或切换到目标分支
- 不考虑任何旧 `status.json` 或旧阶段名的兼容
- `proposal.md` 作为唯一的合并文档，移除 `outline.md`
- 需要同步修改实现、测试、README 与技能文档
